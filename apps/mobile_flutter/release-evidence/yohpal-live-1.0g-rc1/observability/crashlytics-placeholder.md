# Crashlytics Verification — PENDING

Requires: Firebase Crashlytics console screenshot showing at least one captured event
from the release build (triggered via the long-press debug trigger in ThemeSettingsScreen).

Outstanding for both Android and iOS builds.
