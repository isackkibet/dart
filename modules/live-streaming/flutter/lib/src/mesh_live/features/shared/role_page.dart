import 'package:flutter/material.dart';
class RolePage extends StatelessWidget {final VoidCallback onDirector,onCamera;const RolePage({super.key,required this.onDirector,required this.onCamera});@override Widget build(BuildContext context)=>Scaffold(appBar:AppBar(title:const Text('YohPal Mesh Live')),body:Padding(padding:const EdgeInsets.all(24),child:Column(mainAxisAlignment:MainAxisAlignment.center,children:[FilledButton.icon(onPressed:onDirector,icon:const Icon(Icons.switch_video),label:const Text('Use as Director')),const SizedBox(height:16),OutlinedButton.icon(onPressed:onCamera,icon:const Icon(Icons.videocam),label:const Text('Use as Camera Phone'))])));}
